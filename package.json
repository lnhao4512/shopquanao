{
  "scripts": {
    "build": "npm install --prefix client-customer && npm run build --prefix client-customer && npm install --prefix client-admin && npm run build --prefix client-admin && npm install --prefix server",
    "start": "node server/index.js"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "cross-env": "^10.1.0",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^8.0.2",
    "react-router-dom": "^7.13.1",
    "react-scripts": "^5.0.1"
  }
}
