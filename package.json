{
  "scripts": {
    "build": "npm install && cd client-customer && npm install && npm run build && cd ../client-admin && npm install && npm run build && cd .. && mkdir -p public/admin && cp -r client-customer/build/. public/ && cp -r client-admin/build/. public/admin/",
    "start": "node server/index.js"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "express": "^5.2.1",
    "mongoose": "^9.1.4",
    "socket.io": "^4.8.3",
    "dotenv": "^17.2.2",
    "body-parser": "^2.2.2",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^8.0.2",
    "cross-env": "^10.1.0"
  }
}
